# Plan: Ecran principal Sport — rescriere `app/(tabs)/antrenamente.tsx`

## Decizii confirmate (de la tine)
- **Destinație:** rescriu complet `app/(tabs)/antrenamente.tsx` (tab-ul "Sport" rămâne declarat în `_layout.tsx`, nu-l ating). Codul vechi e în git.
- **Hartă spate:** folosesc `<MuscleMapFront side="back" />` pentru ambele fețe (bug-ul din `MuscleMapBack` cu id-uri generice "Vector_N" evitat).

## Sursa unică de adevăr reutilizată (fără a re-inventa)
- `EXERCITII_DB`, `CATEGORII`, `Categorie`, `Exercitiu` din `constants/exercitii.ts` — fiecare exercițiu are deja `muschiTinta: Partial<Record<MuscleId, number>>` populat.
- `MuscleMapFront` din `components/MuscleMapFront.tsx` — props `activeMuscles?: string[]`, `intensity?`, `side='front'|'back'|'both'`. Îi pasez `activeMuscles` (cheile din `muschiTinta` ale exercițiului selectat).
- `useAntrenamente()` din `hooks/useAntrenamente.ts` — `adaugaExercitiu` / `adaugaAntrenament`, tipuri `SetExercitiu`, `ExercitiuInAntrenament`. Persistență AsyncStorage + Supabase (deja există).
- `useTheme()` din `context/ThemeContext.tsx`, tokenii `Radius`/`Spacing`/`Colors` din `constants/theme.ts`.
- `KeyboardAwareScreen`, `CONTENT_BOTTOM_PADDING` din `components/ui/KeyboardAwareScreen.tsx`.
- `useNotify()` pentru toast-uri (pattern din `jurnal-antrenamente.tsx`).

## Iconițe
Folosesc `@expo/vector-icons` (`MaterialCommunityIcons`) conform cerinței tale explicite. Creez un map categorie → nume-icon pentru selectorul orizontal (weight-lifter, dumbbell, run, walk, fire, etc.). Exercițiu curent primește iconița categoriei sale.

## Structura fișierului `antrenamente.tsx` (TypeScript-safe)

```
<KeyboardAwareScreen style={{backgroundColor: colors.background}}>
  <ScrollView contentContainerStyle={{padding: Spacing.lg, paddingBottom: CONTENT_BOTTOM_PADDING}}>

    1. HEADER
       - Titlu "Antrenament" + subtitlu (data de azi)
       - Toggle [Față | Spate] — SegmentedControl-like, setează side: 'front' | 'back'

    2. HARTĂ MUSCULARĂ (container fix height: 350, alignItems center)
       <MuscleMapFront
         side={side}
         activeMuscles={activeMuscles}      // keys din muschiTinta[ex selectat]
         activeColor={colors.accent}
         inactiveColor={colors.surfaceElevated}
         width={SCREEN_WIDTH - 32}
         height={350}
       />

    3. SELECTOR CATEGORII (ScrollView horizontal)
       - Pill-uri cu <MaterialCommunityIcons> + nume (Piept, Spate, Picioare, Umeri, Brațe, Abdomen, Cardio, Full Body)
       - La selectare: setCategory + filtrează exercițiile

    4. LISTĂ EXERCIȚII (ScrollView horizontal, sub categorii)
       - Carduri cu iconița categoriei + nume + grupe scurte
       - La selectare: setSelectedExercise(ex) + setActiveMuscles(Object.keys(ex.muschiTinta ?? {}))
       - Cardul selectat = border accent + bg cyan soft

    5. CARD TRACKER (backgroundColor: colors.surfaceElevated, borderRadius: Radius.lg=16)
       - Titlu: exercițiu selectat (fallback primul exercițiu din listă)
       - 3 rânduri stepper (fiecare cu buton - la stânga, valoare mare centru editabilă TextInput, buton + la dreapta):
         a) Greutate (kg) — step 2.5, min 0
         b) Repetări — step 1, min 0
         c) Setul nr. — auto-incrementat (afișat read-only, următorul = sessionSets.length + 1)
       - Buton mare "Înregistrează Setul" (background #0EA5E9 / colors.accent, height 56, borderRadius pill)
         → la press: append la sessionSets[], toast "Set înregistrat", reset greutate/reps pentru rapiditate, set number++

    6. ISTORIC SESIUNE CURENTĂ
       - Listă seturilor înregistrate la exercițiul curent: "Set 1: 80kg × 10 rep", "Set 2: 85kg × 8 rep"
       - Gol dacă nu există, cu hint "Niciun set înregistrat încă"
       - Buton "Salvează antrenament" → adaugaAntrenament({exercitii}) + toast + clear session

  </ScrollView>
</KeyboardAwareScreen>
```

## State local
```ts
const [side, setSide] = useState<'front'|'back'>('front');
const [selectedCategory, setSelectedCategory] = useState<Categorie>('piept');
const [selectedExercise, setSelectedExercise] = useState<Exercitiu>(...);
const [weight, setWeight] = useState('0');      // string pentru TextInput
const [reps, setReps] = useState('10');
const [session, setSession] = useState<Record<string, SetExercitiu[]>>({}); // exId -> seturi
```
- `activeMuscles` derivat memoizat: `Object.keys(selectedExercise.muschiTinta ?? {})`.
- La schimbare categorie, auto-selectez primul exercițiu din categoria nouă.

## Tipare respectate din codebase
- `StyleSheet.create`, fără className/nativewind.
- `useTheme().colors` pentru toate culorile (sunt pe temă).
- `Haptics.impactAsync(Light)` pe +/- și selecții (pattern din SetLogger).
- Tipuri stricte: fără `any` (TextInput text → number parse cu validare).

## Ce NU fac
- Nu creez tab nou, nu modific `_layout.tsx`, nu repar `MuscleMapBack`, nu ating `SetLogger` (rămâne pentru `exercitiu/[id].tsx`).
- Nu hardcoded `#0F172A` — folosesc `colors.background` (spec-ul tău menționa "ex: #0F172A", dar codebase folosește teme dinamice; apelez la tema curentă). Menționez asta în sumar.
- Butonul "Înregistrează Setul" folosește exact `#0EA5E9` ca accent vizual unic CTA (cerință explicită ta), independent de temă.

## Fișiere atinse
- **Edit:** `frontend-nutritie/app/(tabs)/antrenamente.tsx` (rescris complet).

## Validare
- `npx tsc --noEmit -p frontend-nutritie` (dacă tsconfig există) pentru type-check.
- Raportez rezultatul tău cu sau fără erori.